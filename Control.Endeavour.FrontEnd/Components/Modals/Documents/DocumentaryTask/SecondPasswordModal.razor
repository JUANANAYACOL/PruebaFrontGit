<div class="my-modal">
    <TelerikRootComponent>
        <GenericModalComponent Title=@Translation["SecondPassword"] Width="40%" IsVisible="modalStatus" OnModalClosed="HandleModalClosed">
            <div class="row pt-20">
                <div class="col-12 pt-3">

                    <InputModalComponent LabelText=@Translation["EnterSecondPassword"] @bind-InputValue="tempPassInput" Placeholder=@Translation["SecondPassword"] IsVisible="true" InputType="password"></InputModalComponent>
                    @if (tempPassInput.Count() > 0)
                    {
                        isEnableActionButton = false;
                    }
                    else
                    {
                        isEnableActionButton = true;
                    }
                </div>

                <div class="col-12 pt-3 d-flex justify-content-center">
                    <div class="col-md-3">
                        <ButtonComponent BtnClassColor="btnStyle--primary" BtnType="button" BtnText=@Translation["Accept"] BtnIcon="fa-solid fa-check" BtnDisabled="isEnableActionButton" BtnOnClick="ValidatePassword"></ButtonComponent>
                    </div>

                </div>
            </div>

        </GenericModalComponent>
    </TelerikRootComponent>
    <NotificationsComponentModal @ref="notificationModal" OnModalClosed="HandleModalNotiClose"></NotificationsComponentModal>
</div>